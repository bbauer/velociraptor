<!DOCTYPE html>
<html>
  <head>
    <title>Velociraptor</title>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= stylesheet_link_tag    "http://fonts.googleapis.com/css?family=Open+Sans:300,700" %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">
            <i class="fa fa-comments-o"></i> 
            Velociraptor
          </a>
        </div>

        <% if user_signed_in? %>
          <div class="btn-toolbar pull-right top-nav">
            <div class="btn-group">
              <%= link_to('<i class="fa fa-sign-out"></i> Logout'.html_safe, destroy_user_session_path, method: :delete, class: "btn btn-inverse") %>
              <%= link_to('<i class="fa fa-cog"></i> Settings'.html_safe, destroy_user_session_path, method: :delete, class: "btn btn-inverse") %>
            </div>
          </div>
        <% else %>
          <div class="navbar-collapse collapse">
            <%= form_for("user", :url => user_session_path, html: { class: "navbar-form navbar-right" }) do |f| %>
              <div class="form-group">
                <%= f.text_field :email, class: "form-control", placeholder: "Email" %>
              </div>
              <div class="form-group">
                <%= f.password_field :password, class: "form-control", placeholder: "Password" %>
              </div>
              <%= f.submit 'Sign in', class: "btn btn-primary" %>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>

    <% unless user_signed_in? %>
      <div class="jumbotron">
        <div class="container">
          <%= image_tag("Map.png") %>
          <h1>SEO Software<br />for Awesome People</h1>
          <p>Bacon ipsum dolor sit amet cow pancetta biltong, ham strip steak t-bone shankle sausage. Frankfurter t-bone tenderloin drumstick.</p>
          <p><a class="btn btn-primary btn-lg" role="button">Learn more &raquo;</a></p>
        </div>
      </div>
    <% end %>

    <div class="container">
      <div id="flash-messages">
        <% flash.each do |type, message| %>
          <div class="alert alert-<%= type.to_s %>">
            <a class="close" data-dismiss="alert" href="#">&times;</a>
            <%= message %>
          </div>
        <% end %>
      </div>

      <%= yield %>

      <hr>

      <footer>
        <p>&copy; Velociraptor 2013</p>
      </footer>
    </div>
  </body>
</html>
